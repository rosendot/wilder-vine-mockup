---
/**
 * Compact interior-page hero: breadcrumb trail, eyebrow, title, lede.
 * The home page uses the full-bleed slideshow hero instead.
 */
interface Props {
  /** Label for the current page, shown as the last crumb. */
  current: string;
  eyebrow: string;
  title: string;
  lede: string;
}

const { current, eyebrow, title, lede } = Astro.props;
---

<section class="crumb-bar">
  <div class="wrap">
    <nav class="crumbs" aria-label="Breadcrumb">
      <a href="/">Home</a>
      <span class="sep" aria-hidden="true">/</span>
      <span class="here" aria-current="page">{current}</span>
    </nav>
    <p class="eyebrow">{eyebrow}</p>
    <h1>{title}</h1>
    <p class="lede">{lede}</p>
  </div>
</section>
